
/**
 * Markdown is _supported_ in the magic comments.
 * @param i This describes the first parameter
 * @param j Describing these parameters seems a bit superfluous
 * @returns Add spits out another number, the sum of the 2 parameter numbers
 */
export function add (i: number, j: number): number {
  return i + j
}
